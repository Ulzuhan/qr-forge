# Repository cleanup verification — 2026-09-08

Scope: QR-Forge source cleanup authorized by the maintainer. One Go backend and
React/Vite interface. The initial cleanup did not deploy, tag or push.
The maintainer subsequently authorized committing and pushing this cleanup to
main, including its automatic branch-image publication. No deployment is authorized.

## Evidence from the final working tree

| Check | Result |
|---|---|
| npm install/ci and audit | Clean install; zero reported vulnerabilities after Browserslist 4.28.9 update |
| ESLint / TypeScript | Passed without Next configuration or generated types |
| Frontend unit tests | 8 passed: navigation, short URL, WiFi/email payloads |
| Go race tests | Passed in cmd, auth, httpapi and store |
| Go vet | Passed |
| HTTP contracts | 61 code checks + 12 intent checks passed |
| Back-channel | Synthetic signed-notification suite passed, including live session revocation |
| Browser, Go binary | 28 passed; create, download/decode, edit, redirect, stats, disable, logout, LinkUp intent and mobile |
| Browser, final Docker image | Same 28 passed |
| Historical rollback | 5 + 12 + 8 = 25 passed: pinned Node 0.5.0 → Go → Node, same synthetic SQLite |
| Clean source copy | npm ci, lint, typecheck, build and unit tests passed without prior assets or node_modules |
| Local development startup | Go served 127.0.0.1:13459 with separate .local database; browser rendered the page and reported no errors |
| Runtime inspection | UID 10001; Go executable present; no node/npm commands, node_modules or browser executables found |
| Diff hygiene | git diff --check passed |

The normal rollback port (3991) was already occupied by another server. Nothing
was stopped there; this check used PORT=13991. Browser tests used only their
own synthetic identity provider and disposable stores, not a production account.

The local image tested was qrforge-cleanup:local, image ID
sha256:dd47ccc43370bfc7fbfb1b6099325d592339a33c7c1edf5f91c49a811d62d9f8.
This is a **local image ID**, not a signed registry release digest.

## Removed and retained

Removed the legacy Next backend and duplicate pages/components, Drizzle source
and reset/migration scripts, Node bootstrap and Dockerfile.node. These remain
recoverable in Git history. The old favicon was preserved under public/.

Retained React/Vite/TypeScript/Tailwind, frontend QR generation, Node-based test
harnesses, and the exact historical rollback image reference. Backend security
unit cases were moved to Go; payload tests exercise the functions React uses.
No user data or published rollback images were removed.

README, deployment instructions and CI describe the single Go implementation.
The migration plan is retained with an explicit historical label. Pre-existing
untracked docs/benchmarks notes remain in the workspace, outside this commit;
their old counts and source paths are not current build instructions.

## Remaining external steps

Commit/push was authorized separately after local verification. Main pushes
automatically publish a branch image. Check remote CI on the reviewed commit;
local passing checks are not a claim of a new green GitHub run. Verify the exact
release digest/signature before any deployment. A previously pending real-account
production smoke is still not claimed as completed by these synthetic checks.
